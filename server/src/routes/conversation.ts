// const express = require("express");

// const router = express.Router();

// const checkAuth = require("../middleware/check_Auth");

// const conversationController = require("../controllers/conversation");

// // router.get('/',checkAuth,conversationController.orders_get_all);

// // router.post('/',checkAuth, conversationController.orders_create);

// // router.get('/:orderId',checkAuth, conversationController.orders_get_single);

// // router.delete('/:orderId',checkAuth, conversationController.orders_delete);

// module.exports = router;
