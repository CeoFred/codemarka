import React from 'react';
import { connect } from 'react-redux'

function AttendanceCollector(){
  return (
    <div>
      
    </div>
  );
}
const mapStateToProps = ({ auth }) => {
  return {

  }
}

export  default connect(mapStateToProps,mapDispatchToProps)(AttendanceCollector);